<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DailySong.io - Hafta Detayı</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .song-card { 
            transition: transform 0.2s; 
            border-left: 4px solid #007bff;
        }
        .song-card:hover { transform: translateY(-2px); }
        .winner-card { border-left-color: #ffc107 !important; }
        .navbar-brand { font-weight: bold; }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
        <div class="container">
            <a style="font-size: 24px;" class="navbar-brand" href="/">
                <i class="fas fa-music me-2"></i>DailySong.io
            </a>
            <div class="navbar-nav me-auto">
                <a style="font-weight: bold; color:rgb(204, 203, 203);" class="nav-link" href="/">Ana Sayfa</a>
                <a style="font-weight: bold; color:rgb(204, 203, 203);" class="nav-link" href="/add-song">Şarkı Ekle</a>
                <a style="font-weight: bold; color:rgb(204, 203, 203);" class="nav-link" href="/archive">Arşiv</a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container my-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h1 class="display-4">
                            <i class="fas fa-trophy text-warning me-2"></i>
                            Hafta #<%= week.week_number %>
                        </h1>
                        <p class="text-muted">
                            <%= new Date(week.start_date).toLocaleDateString('tr-TR') %> - 
                            <%= new Date(week.end_date).toLocaleDateString('tr-TR') %>
                        </p>
                    </div>
                    <a href="/archive" class="btn btn-secondary">
                        <i class="fas fa-arrow-left me-2"></i>Arşive Dön
                    </a>
                </div>

                <% if (songs.length === 0) { %>
                    <div class="text-center py-5">
                        <i class="fas fa-music fa-3x text-muted mb-3"></i>
                        <h3 class="text-muted">Bu haftada şarkı eklenmemiş</h3>
                    </div>
                <% } else { %>
                    <div class="row">
                        <% songs.forEach((song, index) => { %>
                            <div class="col-md-6 col-lg-4 mb-4">
                                <div class="card song-card h-100 <%= index === 0 ? 'winner-card' : '' %>">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <span class="badge bg-primary">#<%= index + 1 %></span>
                                            <% if (index === 0) { %>
                                                <i class="fas fa-crown text-warning"></i>
                                            <% } %>
                                        </div>
                                        
                                        <h5 class="card-title text-primary"><%= song.title %></h5>
                                        <h6 class="card-subtitle mb-2 text-muted"><%= song.artist %></h6>
                                        
                                        <p class="text-muted mb-3">
                                            <i class="fas fa-user me-1"></i>
                                            <small><%= song.added_by %></small>
                                        </p>

                                        <!-- Music Links -->
                                        <div class="mb-3">
                                            <% if (song.spotify_url) { %>
                                                <a href="<%= song.spotify_url %>" target="_blank" class="btn btn-sm btn-success me-2">
                                                    <i class="fab fa-spotify"></i> Spotify
                                                </a>
                                            <% } %>
                                            <% if (song.youtube_url) { %>
                                                <a href="<%= song.youtube_url %>" target="_blank" class="btn btn-sm btn-danger">
                                                    <i class="fab fa-youtube"></i> YouTube
                                                </a>
                                            <% } %>
                                        </div>

                                        <!-- Final Vote Count -->
                                        <div class="d-flex justify-content-between align-items-center">
                                            <span class="badge bg-success fs-6">
                                                <i class="fas fa-thumbs-up me-1"></i>
                                                <%= song.votes %> oy
                                            </span>
                                            <small class="text-muted">
                                                <%= new Date(song.created_at).toLocaleDateString('tr-TR') %>
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>